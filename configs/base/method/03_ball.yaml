label:
  method: ball

# BALL uses local CE but it is implemented internally so we turn this off here
use_local_ce: False

peft:
  type: BALL
  beta: 1.0

  vbll: False
  train_samples: 1
  test_samples: 10

  config:
    r: 10
    vbnn:
      prior_mean: 0.0
      prior_sd: 1.0
      init_sd: 0.02
      deterministic_eval: false

hpsearch:
  n_trials: 15
  params:
    "lr": {type: float, low: 1e-3, high: 1e-2, log: True}
    "peft.beta": {type: float, low: 0.01, high: 1.0, log: True}
    # "peft.config.dropout": {type: float, low: 0.0, high: 1.0, log: False}

hpsearch_study_version: 15
